# /status

Give a quick summary of the app's current state.

## What to do
1. Read `CLAUDE.md` for architecture overview
2. List all files in `src/pages/` and `src/components/` 
3. Count total questions in `src/data/questions/` (one per subject)
4. Check `package.json` for dependencies
5. Report:
   - ✅/❌ Each page exists
   - ✅/❌ Key services exist (supabase.js, groq.js, adaptive.js)
   - Total question count
   - Missing env vars (check if .env.local exists)
   - Next steps to get the app running
